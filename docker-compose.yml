version: '3.7'
services:
  app:
    build: .
    container_name: python-web-app
    ports:
      - "5000:5000"

  nginx:
    image: nginx:latest
    container_name: nginx-proxy
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
